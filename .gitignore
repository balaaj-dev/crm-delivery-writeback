# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# local env files — never commit real credentials
.env
.env.local
.env*.local

# logs
npm-debug.log*
*.log

# local demo state (event log, client config overrides) — never commit
/data/

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel
coverage
